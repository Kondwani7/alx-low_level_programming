#include "main.h"
/**
 * _abs - aboslute value of an integar
 * @n: the integar
 * Return: Always 0 for success
 */
int _abs(int n)
{
	if (n < O)
	{
		return (n * (-1));
	}
	else if (n == 0)
	{
		return (0);
	}
	else
	{
		return (n);
	}
}
